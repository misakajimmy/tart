{
  "name": "@tartjs/root",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "prepare": "lerna run prepare",
    "publish": "lerna publish --no-private --skip-git"
  },
  "dependencies": {
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "plugins/*"
  ],
  "devDependencies": {
    "lerna": "^5.3.0"
  }
}
